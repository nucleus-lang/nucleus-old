extern print(content: string) -> int;
extern printi(x: int) -> int;

extern printd(x: double) -> double;

func main(argc: int) -> int
{
	var getFinalInt: int = 0;

	for(i: int = 0; i < 1000000000; 1) 
	{ 
		getFinalInt = i;
	}

	printi(getFinalInt);
	1;
}