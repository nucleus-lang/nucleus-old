use "STD";

shelf STD
{
	shelf File
	{
		func Read(string url) -> string
		{
			var getContents: string;
	
			// Do the magic lol
			// If "getContents" returns nothing, 
			// then it means that there was an error.
	
			return getContents;
		}
	
		func Write(string url, string content) -> bool
		{
			var getStatus: bool;
	
			// Do the magic lol
			// If "getStatus" is false,
			// then it means that there was an error.
	
			return getStatus;
		}
	}
}